JPA 프록시

엔티티를 조회할 때 연관된 엔티티들이 항상 사용되지 않을 수 있다.
엔티티가 실제 사용될 때 까지 데이터베이스 조회를 지연하는 방법을 제공하는데 이것을 지연로딩이라한다.
이기능을 사용하기위해선 실제 엔티티객체대신 프록시 객체를 사용한다.

엔티티를 조회할 때 사용하는 시점까지 데이터베이스 조회를 미룰려면
em.getReference()를 사용한다. 대신 데이터베이스 접근을 위임한 프록시 객체를 반환한다.

프록시 객체의 초기화 여부를 알고 싶다면 PersistenceUnitUtil()메소드를 사용하자.